## How to install

Make sure you are using [pnpm](https://pnpm.io/) (version 11 or later)

In the root folder:

```
pnpm install
```

Then:

```
pnpm compile
```

## Ember

```bash
cd packages/ember
pnpm start
```

## Core

```bash
cd packages/core
# pnpm test --watch for watch mode
pnpm test
```
